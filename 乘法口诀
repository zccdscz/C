#include<stdio.h>
//第一个for循环从1开始循环到9，进入循环中再嵌套一个从1开始到上一个循环的i结束的for循环，然后输出i的值，j的值，i*j的值
//第一个循环i从1开始进入第二个循环后j循环i次每次加一，当j加到比i大的时候循环中止，重新开始i++的循环，再次进入j的循环
//i就可以控制行数，j控制列数，最后以i*j=i*j的形式打印，就形成了乘法口诀表
int main()
{
	int i = 0;
	for (i = 1; i <= 9; i++)
	{
		int j = 1;
		for (j = 1; j <= i; j++)
		{
			printf("%d*%d=%-2d ", i, j, i * j);
		}
		printf("\n");
	}
		

		return 0;

	
}
